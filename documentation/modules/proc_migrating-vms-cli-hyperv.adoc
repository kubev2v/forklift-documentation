// Module included in the following assemblies:
//
// * documentation/doc-Migrating_your_virtual_machines/assemblies/assembly_migrating-from-hyperv.adoc

:_mod-docs-content-type: PROCEDURE
[id="proc_migrating-vms-cli-hyperv_{context}"]

= Running a Microsoft Hyper-V migration from the command-line

[role="_abstract"]
You can migrate from Microsoft Hyper-V as a source provider by using the command-line interface (CLI).

.Prerequisites
* If you are using a user-defined network (UDN), note the name of its namespace as defined in {virt}.

.Procedure
. Configure the source provider:
+
.. Create a `Secret` manifest for the source provider credentials:
+
[source,yaml,subs="attributes+"]
----
$ cat << EOF | {oc} apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: <secret>
  namespace: <namespace>
  ownerReferences:
    - apiVersion: forklift.konveyor.io/v1beta1
      kind: Provider
      name: <provider_name>
      uid: <provider_uid>
  labels:
    createdForProviderType: hyperv
    createdForResourceType: providers
type: Opaque
stringData:
  url: <smb_server_path>
  username: <smb_username>
  password: <smb_password>
EOF
----
+
where:
+
`ownerReferences`::
Optional section for the provider's `name` and `uid`.
+
// SME REVIEW NEEDED: Do unquoted backslashes break YAML parsing? Should we recommend only forward-slash format?
`<smb_server_path>`::
The SMB share path in `//server/share` or `\\server\share` format (for example, `//192.168.122.115/VMShare`).
+
`<smb_username>`::
The username for the SMB share (for example, `Administrator` or `DOMAIN\username`).
+
`<smb_password>`::
The password for the SMB share.

.. Create a `Provider` manifest for the source provider:
+
[source,yaml,subs="attributes+"]
----
$ cat << EOF | {oc} apply -f -
apiVersion: forklift.konveyor.io/v1beta1
kind: Provider
metadata:
  name: <source_provider>
  namespace: <namespace>
spec:
  type: hyperv
  url:  <smb_server_path>
  secret:
    name: <secret>
    namespace: <namespace>
EOF
----
+
where:
+
`<smb_server_path>`::
The SMB share path in `//server/share` or `\\server\share` format (for example, `//192.168.122.115/VMShare`).
+
`<secret>`::
The name of the provider `Secret` CR.

. Map networks and storage:
+
.. Create a `NetworkMap` manifest to map the source and destination networks:
+
[source,yaml,subs="attributes+"]
----
$  cat << EOF | {oc} apply -f -
apiVersion: forklift.konveyor.io/v1beta1
kind: NetworkMap
metadata:
  name: <network_map>
  namespace: <namespace>
spec:
  map:
    - destination:
        name: <network_name>
        type: pod
      source:
        id: <source_network_id>
    - destination:
        name: <network_attachment_definition>
        namespace: <network_attachment_definition_namespace>
        type: multus
      source:
        id: <source_network_id>
  provider:
    source:
      name: <source_provider>
      namespace: <namespace>
    destination:
      name: <destination_provider>
      namespace: <namespace>
EOF
----
+
where:
+
`type`::
The network type: `pod` or `multus`.
+
`<source_network_id>`::
The Hyper-V network adapter (virtual switch) name or ID. The name is the virtual switch friendly name. The ID is the switch GUID in 8-4-4-4-12 hexadecimal format.
+
`<network_attachment_definition>`::
A network attachment definition (NAD) for each additional {virt} network.
+
`<network_attachment_definition_namespace>`::
The namespace of the {virt} NAD. Required only when `type` is `multus`.
+
`namespace`::
The namespace. If you are using a user-defined network (UDN), its namespace is defined in {virt}.

.. Create a `StorageMap` manifest to map source and destination storage:
+
[source,yaml,subs="attributes+"]
----
$ cat << EOF | {oc} apply -f -
apiVersion: forklift.konveyor.io/v1beta1
kind: StorageMap
metadata:
  name: <storage_map>
  namespace: <namespace>
spec:
  map:
    - destination:
        storageClass: <storage_class>
        accessMode: <access_mode>
      source:
        id: <source_storage_id>
  provider:
    source:
      name: <source_provider>
      namespace: <namespace>
    destination:
      name: <destination_provider>
      namespace: <namespace>
EOF
----
+
where:
+
`<access_mode>`::
The access mode: `ReadWriteOnce` or `ReadWriteMany`.
+
`<source_storage_id>`::
The Hyper-V storage identifier (file path, volume GUID path, or storage pool name). For example, `C:\VHDs\disk.vhdx`.

. Optional: Configure migration hooks:
+
.. Create a `Hook` manifest to run custom code on a VM during the phase specified in the `Plan` CR:
+
[source,yaml,subs="attributes+"]
----
$  cat << EOF | {oc} apply -f -
apiVersion: forklift.konveyor.io/v1beta1
kind: Hook
metadata:
  name: <hook>
  namespace: <namespace>
spec:
  image: quay.io/kubev2v/hook-runner
  serviceAccount: <service_account>
  playbook: |
    LS0tCi0gbm...
EOF
----
+
where:
+
`<service_account>`::
The {ocp} service account (optional). Use this parameter to modify cluster resources.
+
`playbook`::
The Base64-encoded Ansible Playbook. If you specify a playbook, the `image` must include an `ansible-runner`.
+
[NOTE]
====
You can use the default `hook-runner` image or specify a custom image. If you specify a custom image, you do not have to specify a playbook.
====

. Configure the transfer network:
+
.. Create the network attachment definition (NAD) for the transfer network to configure the interface IP address (DHCP or static) and enable gateway access:
+
[source,yaml,subs="attributes+"]
----
$ oc edit NetworkAttachmentDefinitions <name_of_the_NAD_to_edit>
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: <name_of_transfer_network>
  namespace: <namespace>
  annotations:
    forklift.konveyor.io/route: <IP_address>
----

. Create and run the migration plan:
+
.. Create a `Plan` manifest for the migration:
+
[source,yaml,subs="attributes+"]
----
$ cat << EOF | {oc} apply -f -
apiVersion: forklift.konveyor.io/v1beta1
kind: Plan
metadata:
  name: <plan>
  namespace: <namespace>
spec:
  provider:
    source:
      name: <source_provider>
      namespace: <namespace>
    destination:
      name: <destination_provider>
      namespace: <namespace>
  map:
    network:
      name: <network_map>
      namespace: <namespace>
    storage:
      name: <storage_map>
      namespace: <namespace>
  targetNamespace: <target_namespace>
  vms:
    - id: <source_vm1>
    - name: <source_vm2>
      hooks:
        - hook:
            namespace: <namespace>
            name: <hook>
          step: <step>
EOF
----
+
where:
+
`<plan>`::
The name of the `Plan` CR.
+
`map`::
One network map and one storage map per plan.
+
`network`::
A network mapping. Required even if VMs are not assigned to a network (the mapping can be empty).
+
`<network_map>`::
The name of the `NetworkMap` CR.
+
`storage`::
A storage mapping. Required even if VMs are not assigned disk images (the mapping can be empty).
+
`<storage_map>`::
The name of the `StorageMap` CR.
+
`vms`::
The source VMs and their hooks. Use `id` or `name` to specify VMs. If using a UDN, verify the provider IP address is outside the UDN subnet to avoid migration failure.
+
`<source_vm1>`::
The Hyper-V VM identifier.
+
`hooks`::
Up to two hooks per migration (optional). Each hook must run during a separate migration step.
+
`<hook>`::
The name of the `Hook` CR.
+
`<step>`::
The hook type: `PreHook` (before migration) or `PostHook` (after migration).

// SME REVIEW NEEDED: Is the cutover parameter supported for Hyper-V cold migrations, or should it be removed?
.. Create a `Migration` manifest to run the `Plan` CR:
+
[source,yaml,subs="attributes+"]
----
$ cat << EOF | {oc} apply -f -
apiVersion: forklift.konveyor.io/v1beta1
kind: Migration
metadata:
  name: <name_of_migration_cr>
  namespace: <namespace>
spec:
  plan:
    name: <name_of_plan_cr>
    namespace: <namespace>
  cutover: <optional_cutover_time>
EOF
----
+
[NOTE]
====
If you specify a cutover time, use the ISO 8601 format with the UTC time offset, for example, `2024-04-04T01:23:45.678+09:00`.
====
