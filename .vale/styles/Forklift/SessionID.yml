# Merge blocker: do not use "SessionID" or "Session ID" in Forklift documentation.
extends: existence
message: "Do not use '%s' in documentation. Use the approved term instead."
level: error
tokens:
  - SessionID
  - Session ID
